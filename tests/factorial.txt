program factorial;

int function fact(n: int) {
  if(n == 0) then {
    return (1);
  }

  return (n * factorial(n - 1));
}

main() {
  var num: int;
  print("Insert number");
  read(num);
  print(factorial(num));
}