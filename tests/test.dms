program Team_pp;
class Person
{
    attributes
    var age: int;
    var name: string;
    methods
    int function UNO(x: int) {
        return (age - x);
    }
    void function DOS(f: float) {
        print(age + f);
    }
}

class Paola inherits Person
{
    attributes
    methods
}

var i, j, p, k: int;
var matrix[2, 3] : int;
var student: Person;

void function pelos(y: int) {
    var x: int;
    read(student.age);
    student.DOS(x);
    x = y;
    while(x < 10) do {
        print(student.UNO(x));
        if(y < 5) then {
            x = x + 1;
        } else {
            x = x + 2;
        }
    }
}

int function fact(j: int) {
    var i: int;
    var foo: bool;
    var a,b,c,d,e, f,l, k, h, g: int;

    i = j + k - (p - j * 2 / (3 + j)) * 4;
    foo = 1 + 2 < 2 | 2 > 0 & i > j * 2 / 3;
    foo = (1 + 2 / 3 - 5 * 4) > (i + j / (6 - 8) * 9);
    foo = a + b * e + d > a * e;
    foo = ((a + b)* c + d * e * f + k / h * j) + g * l + h + j > (a - c * d) / f;
    pelos(i);
    if(j == 1) then {
        i = j;
        return (j);
    } else {
        i = i / j;
        return (j * fact(j - 1));
    }
}

main() {
    read(p);
    j = p * 2;
    i = fact(p);
    read(student.name);
    pelos(p);
    for i = 1 until 10 do {
       print("Hello World", student.name, fact(student.age));
        print("Hello World", student.name);
        print("Hello World", 1 + 2);
    }
}
